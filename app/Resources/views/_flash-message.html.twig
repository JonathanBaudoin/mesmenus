{% for type, content in app.session.flashBag.all %}
    <div class="flash-message flash-{{ type }}">
        {% for message in content %}
            {{ message }}
        {% endfor %}
    </div>
{% endfor %}